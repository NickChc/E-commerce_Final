import styled from "styled-components";
import tw from "twin.macro";

export const SCheckoutProducts = styled.div`
  ${tw`w-full flex flex-col items-stretch gap-y-2 h-full min-h-[8rem] overflow-y-auto overflow-x-hidden `}
`;
