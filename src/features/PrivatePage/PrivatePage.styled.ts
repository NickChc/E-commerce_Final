import styled from "styled-components";
import tw from "twin.macro";

export const SNavigateWrapper = styled.div`
  ${tw`min-h-dvh w-full flex items-center justify-center text-[4rem] pb-20 gap-x-6 `}

  h1 {
    ${tw`text-[3rem] `}
  }
`;
